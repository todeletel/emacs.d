
;; org-agenda
(setq org-agenda-files (list "~/mylifedoc/todo.org" 
			     "~/ele_org/project.org"))

(require 'org)
(setq org-src-fontify-natively t)
;;(setq org-log-done 'time)
;;todo-log
(provide 'init-org)
